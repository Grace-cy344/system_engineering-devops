#!/usr/bin/env bash
#
# Display "Best School" 10 times, but say "Hi" on the 9th iteration

count=1
while [ "$count" -le 10 ]; do
    printf '%s\n' "Best School"
    if [ "$count" -eq 9 ]; then
        printf '%s\n' "Hi"
    fi
    count=$((count + 1))
done
